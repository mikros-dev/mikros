name = "http"
types = ["http:8080"]
version = "v0.1.0"
language = "go"
product = "EXAMPLE"
envs = ["SOME_VALUE"]

[log]
#error_stack_trace = "structured"
error_stack_trace = "disabled"

[runtime.http]
disable_auth = true

